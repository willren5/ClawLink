# 2026-03-06

- Audited the ClawLink system-surfaces, notifications, deep link, chat streaming, and iOS widget stack.
- Verified TypeScript with `npm run typecheck`.
- Verified Widget Extension Swift compilation via `xcodebuild` until the build moved into app-level storyboard tooling.
- Added system-surface stats fields for `costYesterday`, `requestsToday`, and `tokenUsageToday`.
- Added native `getGatewayStatus` bridge method for App Group snapshot reads.
- Added `ClawLinkCostWidget` and registered it in the widget bundle.
- Added `ClawLinkMultiGatewayWidget`, background multi-gateway health polling, App Intents shortcut queue handling, and Health Bridge M1 UI/permission flow.
- Remaining native build failure in this environment is `CompileStoryboard` for `SplashScreen.storyboard` because `ibtool/CoreSimulatorService` is unavailable in sandbox.
- Added app-target App Intents for gateway status, restart-agent, and send-message shortcut entry points.
- Added multi-gateway widget plumbing plus a medium widget that reads `multi-gateway:status` from App Group.
- Added Health Bridge M1 UI: settings entry, persisted local policy store, permission request UI, and mock payload preview.
- Fixed Dynamic Island info density: compact view now prefers pending sync / cost / freshness over `Sxx Qx`, and expanded regions opt into `belowIfTooWide` placement to avoid cutout overlap.
- Fixed onboarding permission flow so Local Network is treated as part of the first-run permission step instead of being deferred until gateway connect.
- Added native iOS Local Network preflight via `NWBrowser` + optional host probe, plus `NSBonjourServices` entry for proactive prompting.
- Fixed Health Bridge debug entitlement mismatch and surfaced HealthKit authorization failures instead of silently swallowing them.
- Improved dashboard readability for gateway health and 7D selector, and removed the oversized chat composer bottom inset that caused the gap above the tab bar.
- Built two Xiaohongshu promo cover variants from local ClawLink screenshots: a provocative "别做手机版，先做 iPhone 遥控器" version and a stronger pain-hook "Agent 挂了，手机上直接救火" version under `.tmp/promo/output/`.
- Revised the main Xiaohongshu cover per user feedback: headline now uses "OpenClaw挂了 / 躺在床上 / 用手机救火", the support line emphasizes widget token spend, and one lower screenshot was replaced with the app icon card.
- Removed the top-right "不是概念图 / 直接用真实截图做封面" sticker from the main Xiaohongshu cover template and re-rendered the final PNG.
